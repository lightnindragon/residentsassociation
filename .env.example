# Copy this file to `.env.local` and fill in real values. Never commit `.env.local`.
# These variables are server-only (API routes, Server Components, middleware); they are not exposed to the browser.

# Neon Postgres – https://neon.tech
DATABASE_URL="postgresql://[user]:[password]@[neon_hostname]/[dbname]?sslmode=require"

# NextAuth – generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="your-secret"
# Use your real site URL in production (e.g. https://cagra.co.uk). Wrong value breaks auth and can break
# homepage hero images if the DB stores an absolute URL to this host (see next.config.ts remotePatterns).
NEXTAUTH_URL="http://localhost:3000"

# Optional: public site URL — adds Image remotePatterns for this host (same as NEXTAUTH_URL if unset).
# NEXT_PUBLIC_SITE_URL="https://cagra.co.uk"

# Encrypt SMTP password in DB – long random string (same generator as NEXTAUTH_SECRET is fine)
ENCRYPTION_KEY="your-encryption-key"

# Vercel Blob – server-only uploads (gallery, hero seed, etc.). Public image URLs work in the browser
# without this token; if hero/gallery images 400 on production, fix next.config.ts remotePatterns for Blob
# (see *.public.blob.vercel-storage.com), not this variable.
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# Optional: after `npm run upload-site-logo`, set this so the header uses Blob instead of /branding/site-logo.jpeg
# NEXT_PUBLIC_SITE_LOGO_URL="https://….public.blob.vercel-storage.com/site/main-logo.jpeg"
